<%= form_for([@post, @post.comments.build]) do |f| %> 
  <p>
    <%= f.label 'comment' %>
     <%= f.text_area :body, required: true %>
  </p>
  <p>
    <%= f.submit 'comment' %>
  </p>
<% end %>