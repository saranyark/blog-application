<div class="cmnts">
  <strong><%= comment.body.html_safe %></strong>
  <p class="text-muted">posted <%= time_ago_in_words(comment.created_at) %> ago</p>
 
    <p><%= link_to 'Delete',[comment.post, comment],class: 'btn btn-xs btn-danger', method: :delete, data: {confirm: "Are you sure?"} %></p>
 
</div>